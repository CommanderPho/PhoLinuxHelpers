# Wanted to use qdirstat on RHEL to visualize space issues (both of local disks and GDrives).
This didn't work: It isn't available for RHEL, the commands for other linux distros don't work to get setup for building it from source. 
For example, `qmake` is missing and not available through `yum` or `dnf`. A suggestion online was to install Qt from the website to a global directory, so I'm doing this now to `/media/MAX/SlowSwap/Qt` isn't available for RHEL

# Installing Qt as
https://access.redhat.com/discussions/4142491

## QDirStat Appimage?
https://github.com/area-of-dev/QDirStat.AppImage
https://github.com/shundhammer/qdirstat/issues/168

## QDirStat Docker Image?
https://hub.docker.com/r/jlesage/qdirstat


https://snapcraft.io/store



After install, `qmake` still not found

